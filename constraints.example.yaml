depot:
  name: USF CUTR
  lat: 28.0585626
  lon: -82.4163460

limits:
  max_hours_per_night: 4.0
  max_nights: 200

speed:
  service_factor: 1.0
  deadhead_factor: 1.0

lane_split:
  strategy: balanced  # lanes_A=ceil(total/2), lanes_B=floor(total/2)

oneway:
  default: false

loopback:
  mode: constant      # constant | routing
  constant_seconds: 60

objective:
  primary: min_deadhead_distance
  secondary: min_deadhead_time
